# 开发记录

更新时间：2026-07-13

## 2026-07-13 第一周技术 SEO 改造

- 导出并保存 GSC 原始 CSV，记录 Cloudflare 近 7 天 PV、visits、referrer、landing page、hostname 和 Web Vitals 基线。
- 诊断 GSC sitemap“无法读取”：站点端 GET/HEAD/Googlebot/XML/robots/canonical/IPv4/IPv6 全部正常；5 个 URL Inspection 显示首页和 Performance 已收录，其余 3 个代表页面尚未被 Google 识别。
- 标准 sitemap 增加 lastmod、XML 转义和缓存；新增含 3 个视频的 video sitemap，robots 同时声明两个 sitemap。
- `/guides/?q=` 改为真实站内搜索，索引所有文字攻略和视频；7 组查询、无结果、清空和键盘提交通过真实浏览器验收。
- 31 个正式页面 title ≤60、description ≤160 且均无重复；首页和 5 个聚合页补齐与实际内容一致的 WebSite/CollectionPage/ItemList。
- 3 个视频页新增 27 个 Clip；`?t=130` 实际定位到 Orserk 章节，播放器 191.48 秒、1920×1080、`readyState=4`。
- 对 R2、Stream、Pages 完成当前官方价格和能力比较，确定第四条视频前迁移到 R2 Standard + 自有媒体子域名 + Cloudflare Cache；资源创建仍需用户授权。
- `npm run build`、XML、canonical、内部链接和结构化数据静态检查全部通过；详细证据位于 `reviews/2026-07-13_week1_execution/`。

## 2026-07-13 第三个英文视频攻略

- 新增 `Level 8 Worker Pals & Locations` 独立视频页与独立完整文字攻略页，双方互链但文字页不嵌播放器。
- 站内 HLS 为 1920×1080、60fps、H.264/AAC、32 个 fMP4 分片；最大分片约 5.54 MB，全片解码通过。
- YouTube 备份 `_1daH8dpyWw` 在内容列表明确显示 Unlisted；公开站点继续保持 0 YouTube iframe。
- 视频页提供 12 个章节和 source-footage 验证说明；文字页解释坐标差异、Lv.7 Transporting caveat、Panthalus 非工作 Pal 等准确性边界。
- 首页按用户参考图完成视觉对照，新视频进入右上主推荐位，三条真实视频在独立视频版块，工具和书面攻略保持分区。
- 1488×1057、390×844、HLS readyState、2:10 章节、SEO、sitemap 和 31 页面 build 本地检查通过。
- commit `85a028b` 自动部署为 Cloudflare Pages production `bd06e946`；生产首页、视频索引、视频页、文字页、HLS 和 sitemap 的 HTTP/MIME/SEO 检查通过。
- 线上 Chrome 真实播放通过：`readyState=4`、191.48 秒、1920×1080、0 iframe；2:10 Orserk 章节精确跳到 130 秒并继续播放，2.5 秒内前进约 2.53 秒；390px 无横向溢出，文字页 0 player/0 VideoObject。

## 2026-07-12 视频与文字攻略架构拆分

- 新增 `/videos/` 视频总览和两个独立视频详情页，现有两条视频集中进入单独版块。
- 原 `/guides/` 页面保留完整英文文字攻略，移除播放器和 `VideoObject`，并与对应视频页双向链接。
- 将视频元数据独立到 `src/data/videos.ts`；首页 Videos 与 written guides 改为两个清晰版块。
- 本地构建生成 29 个页面；两条 HLS 的真实播放和章节跳转通过，390px/1280px 无横向溢出，文字页确认 0 播放器和 0 VideoObject。
- 将相同架构规则写回全局 `localize-short-video` Skill、SEO checklist 和端到端 checklist，并通过 Skill 校验。
- commit `b2d82ad` 自动部署为 Cloudflare Pages production `90ed9aa2`；生产域名上的视频索引、两个视频页、两个文字页、sitemap 和 HLS 均完成 200/MIME/SEO 检查。
- 线上未登录 Playwright 验收通过：两条视频均可播放并完成章节跳转，`readyState=4`、1920×1080、0 iframe、0 控制台错误；390px 无横向溢出，文字页保持 0 player/0 VideoObject。

## 2026-07-12 首个英文视频攻略

- 完成授权中文短视频的 4K 英文化成片、英文字幕、缩略图和当前 Palworld 1.0 关键路线复测。
- 新增完整英文图文攻略、8 个章节按钮、测试信息、VideoObject、OG 和 sitemap。
- 初版 YouTube Unlisted iframe 被真实访客要求登录确认非机器人，随后改为站点自托管 1080p60 H.264/AAC HLS。
- 46 个 6 秒 fMP4 分片全部低于 Cloudflare Pages 单文件限制，全片解码通过。
- 未登录线上浏览器实际播放、0:53 章节跳转、0 iframe、0 控制台错误和 390px 响应式均通过。
- GitHub `main` commit `b1c0cde` 部署为 Cloudflare production `7c81ca68`；后续 commit `c9619a6` 从公开页面移除 YouTube 备份直链。

## 2026-07-08 Analytics 和 Search Console 接入

### 本次目标

接入 Cloudflare Web Analytics 或 GA4，并在 Google Search Console 添加站点、验证所有权、提交 sitemap。

### 完成内容

- 在 Cloudflare Web Analytics 创建 `palworld-1-0-guide-hub.pages.dev` 站点并取得 beacon snippet。
- 将 Cloudflare Web Analytics 脚本加入全站 `BaseLayout.astro`。
- 添加 Google Search Console HTML 验证文件 `public/google188a703d2dd73306.html`。
- 运行 `npm run build` 成功，仍生成 18 个页面。
- 推送 commit `aee749d` 到 GitHub `main`，Cloudflare Pages 自动部署成功，deployment short id `6aeb7ca0`。
- 线上验证文件返回正确内容，首页源码包含 Cloudflare Web Analytics 脚本。
- Google Search Console URL-prefix 资源验证通过。
- 已在 Search Console 提交 `https://palworld-1-0-guide-hub.pages.dev/sitemap.xml`。

### 遇到问题

- Search Console 提交 sitemap 后即时状态显示“无法抓取”。
- 站点端复查显示 sitemap 对 Googlebot UA 返回 200、`Content-Type: application/xml`，robots 正确声明 sitemap。
- 当前判断为 Google Search Console 后台即时抓取/缓存状态问题，需稍后复查。

### 下一步

- 稍后回到 Google Search Console 复查 sitemap 状态是否变为成功。
- Palworld 1.0 正式 patch notes 发布后更新文章事实。

## 2026-07-07

### 本次目标

完成 Palworld 1.0 Guide Hub MVP：站点结构、首页、分类页、文章模板、10 篇文章初版和 build 检查。

### 完成内容

- 初始化项目文档。
- 创建 Astro 静态站。
- 创建基础 SEO Layout、Header、Footer、GuideCard、ArticleTemplate。
- 写入 8 个核心页面。
- 写入 10 篇文章预备版本。
- 添加非官方声明和谨慎内容提示。
- 运行 `npm run build` 成功，生成 18 个页面。
- 完成本地 SEO 字段统计检查，核心 SEO 标签无缺失。
- 使用 Playwright 检查首页 390px 手机和 1280px 桌面 viewport，无页面级横向溢出。

### 修改文件

- `package.json`
- `astro.config.mjs`
- `scripts/generate-assets.mjs`
- `src/**`
- `docs/**`
- 项目根文档

### 遇到问题

- 当前用户打开的 `/Users/zousunquan/Documents/Palworld 游站` 目录只有 `.git`，没有实际 Astro 项目。按全局工作台规则，项目初始化到 `/Users/zousunquan/Desktop/Codex/projects/Project_02_palworld_1_0_guide_hub/`。

### 下一步

- 部署后设置真实站点 URL。
- Palworld 1.0 正式发布后按官方 patch notes 更新文章。

## 2026-07-07 部署收尾

### 本次目标

把当前 MVP 推送到 GitHub，并继续完成 Cloudflare Pages 部署配置。

### 完成内容

- 使用仓库专用 SSH deploy key 将 `main` 推送到 `git@github.com:zsq378554827/palworld-1-0-guide-hub.git`。
- 通过 Cloudflare Pages Project API 设置 production 和 preview 的普通环境变量 `PUBLIC_SITE_URL=https://palworld-1-0-guide-hub.pages.dev`。
- 复查线上首页、`robots.txt`、`sitemap.xml`、canonical 和 Open Graph，均指向 pages.dev 生产域名。
- 再次运行 `npm run build`，构建通过并生成 18 个页面。

### 遇到问题

- 当前 Cloudflare Pages 项目是 Direct Upload 项目，API 不允许原地更新 GitHub `source`，因此不能直接切换为 Git Integration。

### 下一步

- 由用户决定是否删除/重建同名 Pages 项目来获得 Git 集成，或新建另一个 Git 集成项目。
- Palworld 1.0 正式 patch notes 发布后更新文章事实。

## 2026-07-07 Git 集成重建

### 本次目标

删除当前同名 Direct Upload Pages 项目，重建为 GitHub 集成 Pages 项目，继续使用 `palworld-1-0-guide-hub.pages.dev`。

### 完成内容

- 先用临时 Git 集成项目验证 Cloudflare API 可访问 GitHub 仓库 `zsq378554827/palworld-1-0-guide-hub`。
- 删除临时验证项目。
- 删除旧的 `palworld-1-0-guide-hub` Direct Upload Pages 项目。
- 重建同名 GitHub 集成 Pages 项目。
- 设置构建参数：Framework preset Astro，Build command `npm run build`，Build output directory `dist`。
- 设置 production 和 preview 环境变量 `PUBLIC_SITE_URL=https://palworld-1-0-guide-hub.pages.dev`。
- 首次 production deployment 构建成功，deployment short id `930e27b2`。
- 复查主域名、首页 canonical、Open Graph、`robots.txt` 和 `sitemap.xml`。
- 推送真实文档变更 commit `36838ad` 后，Cloudflare 未自动生成对应 deployment。

### 下一步

- 用户登录 Cloudflare Dashboard 后，用 Connect to Git 流程修复/重建 GitHub webhook，确保后续 push 自动部署。
- Palworld 1.0 正式 patch notes 发布后更新文章事实。

## 2026-07-07 GitHub 自动部署修复

### 本次目标

补齐 Cloudflare Workers and Pages GitHub App 对当前仓库的授权，并验证 GitHub push 能自动触发 Cloudflare Pages production deployment。

### 完成内容

- 在 GitHub App `Cloudflare Workers and Pages` 安装页中，将 `zsq378554827/palworld-1-0-guide-hub` 加入 selected repositories 并保存。
- 推送验证提交 `4028de7` 到 GitHub `main`。
- Cloudflare Pages 自动创建 production deployment，trigger 为 `github:push`，deployment short id `266a3200`，最终状态 `success`。
- 再次运行 `npm run build`，本地构建通过并生成 18 个页面。

### 下一步

- Palworld 1.0 正式 patch notes 发布后更新文章事实。
- 后续接入 Analytics 和 Search Console。
